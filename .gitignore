node_modules/
.env
.env.*
!.env.example
credentials.json
*.json.key
dark-sensor-*.json
d3479eb6-*.json
.DS_Store

# ไฟล์สื่อการสอนตอน dev (production ใช้ object storage)
# ต้องมี / นำหน้า ไม่งั้นกิน lib/storage/ ที่เป็นโค้ดจริงไปด้วย
/storage/

# ค่าเดิมของแถวที่แก้มือบน production + สคริปต์ครั้งเดียวที่ใช้แก้
# เป็นข้อมูลจริงของโรงเรียน เก็บไว้ในเครื่องเผื่อย้อน ไม่ขึ้น GitHub
/db/backups/
