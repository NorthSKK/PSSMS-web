{
  "name": "pssms-web",
  "version": "1.0.0",
  "description": "PSSMS Web Prototype — Express + Google Sheets API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pretest": "node db/seed-dev.js",
    "test": "node --test --test-concurrency=1 \"test/**/*.test.js\"",
    "pretest:only": "node db/seed-dev.js",
    "test:only": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "multer": "^2.2.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
