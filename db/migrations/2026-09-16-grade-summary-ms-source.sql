-- grade_summary.ms_source — แถวนี้ใครเขียน
--
-- 'auto' = ระบบเติม มส. ให้เองเพราะเวลาเรียนต่ำกว่า 80% (functions/autoMs.js)
-- NULL   = ครูเป็นคนเขียน (ปพ.5 / backfill จากคะแนนดิบ)
--
-- ต้องแยกให้ออก เพราะระบบต้อง "ถอนคืน" แถวที่ตัวเองเติมได้เมื่อ % กลับขึ้น ≥80
-- โดยห้ามแตะเกรดที่ครูตัดสินเอง — ถ้าไม่มีคอลัมน์นี้ การถอนคืนจะแยกไม่ออกระหว่าง
-- "มส. ที่ระบบเดาให้" กับ "มส. ที่ครูตั้งใจให้" แล้วต้องเลือกระหว่างลบของครูทิ้ง
-- หรือปล่อยแถวผีค้างตลอดกาล
ALTER TABLE grade_summary ADD COLUMN IF NOT EXISTS ms_source TEXT;
